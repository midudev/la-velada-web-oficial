---
import { ClientRouter } from "astro:transitions"

import ButtonUp from "@/components/ButtonUp.astro"

// import Header from "@/components/Header.astro"
// import KonamiCode from "@/components/KonamiCode.astro"
// import LightsBackground from "@/components/LightsBackground.astro"
import SEO from "@/components/SEO.astro"
import type { Preload } from "@/types/Preload"

import Toast from "@/components/ui/Toast.astro"

// import Footer from "@/sections/Footer.astro"
// import Sponsors from "@/sections/Sponsors.astro"
import "../global.css"

interface Props {
	title: string
	description: string
	preload?: Array<Preload>
	canonical?: string
	image?: string
}

const { title, description, preload, canonical, image } = Astro.props
---

<!doctype html>
<html lang="es" class="overflow-x-hidden">
	<head>
		<SEO
			canonical={canonical}
			description={description}
			image={image}
			preload={preload}
			title={title}
		/>
		<ClientRouter />
	</head>

	<body
		class="selection:bg-primary selection:text-secondary [&_:focus-visible]:ring-primary overflow-x-hidden [&_:focus-visible]:ring-2 [&_:focus-visible]:outline-none"
	>
		<a
			href="#main-content"
			class="anchor-main-content bg-secondary text-primary fixed top-0 z-20 rounded-br-md px-3 py-5 opacity-0 focus:opacity-100"
			>Saltar al contenido principal</a
		>
		<!-- <NoiseBackground /> -->
		<!-- <LightsBackground /> -->
		<!-- <Header /> -->
		<div id="main-content">
			<slot />
			<!-- <Sponsors />
			<Footer />
			<KonamiCode /> -->
			<Toast />
			<!-----------------------------
     `    ``               `
      ``  _ `      `       ``
     `   |_| `  `` ``    `  `
    ``  -___-_` `   ` --------------
  ``   /      )      | This is fine |`
 `____/| (0) (0)_()  |/-------------  `
/|   | |   ^____)      ``      ``
||   |_|    \_//     Uɔ````   `` ``
||    || |    |    ========`  ``  ``
||    || |    |      ||     ``   `
||     \\_\   |\     ||   ```    `
=========||====||    ||  ``       `
  || ||   \Ɔ || \Ɔ   ||   ``    ``
  || ||      ||      ||  `     ``
--------------------------------->
		</div>
		<ButtonUp />
	</body>
</html>
