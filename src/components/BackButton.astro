<div class="absolute left-2 top-32 z-10 hidden md:block" transition:name="back button">
	<button
		aria-label="Ir atrás"
		class="back-btn group relative m-0 block size-14 cursor-pointer overflow-hidden border-0 bg-transparent outline-none focus-visible:rounded-full"
	>
		<div
			class="absolute left-0 top-0 flex group-hover:-translate-x-14 group-hover:duration-[0.4s] group-focus:-translate-x-14 group-focus:duration-[0.4s]"
		>
			<span class="mx-[18px] mb-0 mt-[17px] block size-5 rotate-180 fill-primary stroke-1">
				<svg viewBox="0 0 46 40">
					<symbol id="back-arrow">
						<path
							d="M46 20.038c0-.7-.3-1.5-.8-2.1l-16-17c-1.1-1-3.2-1.4-4.4-.3-1.2 1.1-1.2 3.3 0 4.4l11.3 11.9H3c-1.7 0-3 1.3-3 3s1.3 3 3 3h33.1l-11.3 11.9c-1 1-1.2 3.3 0 4.4 1.2 1.1 3.3.8 4.4-.3l16-17c.5-.5.8-1.1.8-1.9z"
						></path>
					</symbol>
					<use href="#back-arrow"></use>
				</svg>
			</span>
			<span class="mx-[18px] mb-0 mt-[17px] block size-5 rotate-180 fill-accent">
				<svg viewBox="0 0 46 40">
					<use href="#back-arrow"></use>
				</svg>
			</span>
		</div>
	</button>
</div>

<script is:inline>
	document.querySelector(".back-btn")?.addEventListener("click", () => {
		window.history.back()
	})
</script>

<style>
	.back-btn:before,
	.back-btn:after {
		content: "";
		position: absolute;
		border-radius: 50%;
		inset: 7px;
	}

	.back-btn:before {
		border: 1px solid var(--color-primary);
		transition:
			opacity 0.4s cubic-bezier(0.77, 0, 0.175, 1) 80ms,
			transform 0.5s cubic-bezier(0.455, 0.03, 0.515, 0.955) 80ms;
	}

	.back-btn:after {
		border: 1px solid var(--color-accent);
		transform: scale(1.3);
		transition:
			opacity 0.4s cubic-bezier(0.165, 0.84, 0.44, 1),
			transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
		opacity: 0;
	}

	.back-btn:hover:before,
	.back-btn:focus:before {
		opacity: 0;
		transform: scale(0.7);
		transition:
			opacity 0.4s cubic-bezier(0.165, 0.84, 0.44, 1),
			transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	.back-btn:hover:after,
	.back-btn:focus:after {
		opacity: 1;
		transform: scale(1);
		transition:
			opacity 0.4s cubic-bezier(0.77, 0, 0.175, 1) 80ms,
			transform 0.5s cubic-bezier(0.455, 0.03, 0.515, 0.955) 80ms;
	}
</style>
